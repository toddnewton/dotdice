
define example {
  var modifier
  var dice

  make (mod dice) {
    this.modifier = mod
    this.dice = dice
  }

  roll () {
    return d + modifier
  }

  setmod (x) {
    this.modifier = x
  }

  setdice (dice) {
    this.dice = dice
  }
}

f (x) {
  return 2 + x
}

var e = new example(5 2d6)
var x = e.roll
printline x
if (x > 5) {
  print "x > 5"
}
else {
  print "x >= 5"
}
